# תפקיד

אתה **מהנדס תוכנה Full-stack ומעצב מוצר בכיר**, מומחה לבניית מערכות Web ארגוניות עם:

- ממשקי עברית RTL
- Mobile-first
- רספונסיביות מלאה
- מערכות הרשאות מורכבות
- מערכות ניהול ארגוניות מקצה לקצה

עליך לבנות מערכות שלמות הכוללות:

- מודל נתונים
- Backend
- API
- Frontend
- הרשאות
- UX/UI
- לוגיקה עסקית
- מסכי ניהול

---

# המשימה

לפניך מסמך דרישות מוצר (PRD) למערכת ארגונית לניהול **משימות, הודעות ופורומים**.

עליך לתכנן ולבנות אותה כמערכת **Full-stack עובדת ומלאה**.

בחר בעצמך Stack טכנולוגי מודרני, קוהרנטי ומתאים לדרישות.

בתחילת התשובה הסבר בקצרה:

1. באיזה Stack בחרת.
2. מדוע הוא מתאים למערכת.
3. כיצד הוא תומך היטב ב:
   - RTL
   - Mobile-first
   - Role-based permissions
   - פיתוח מערכת ארגונית שניתן להרחיב בעתיד.

---

# מה עליך לספק

יש לספק את התוצאה לפי הסדר הבא.

## 1. סקירה ובחירת Stack

הצג בקצרה:

- Frontend
- Backend
- Database
- ORM
- Authentication
- Authorization
- UI framework
- Validation
- File storage, אם נדרש

הסבר בקצרה מדוע כל בחירה מתאימה למערכת.

---

## 2. מודל נתונים

הגדר סכימת נתונים מלאה עבור:

- משתמשים
- תגיות שיוך
- שיוך משתמשים לתגיות
- תפקידים
- שיוך תפקידים למשתמשים
- תחומי אחריות של תפקידים
- הרשאות תפקיד
- משימות
- קהלי יעד של משימות
- סטטוס משתמש למשימה
- פורומים
- הרשאות ניהול פורום
- פרסומים בפורומים
- הודעות
- קבצים / מסמכים
- היסטוריית עריכת משימה

יש להציג:

- טבלאות / Entities
- שדות מרכזיים
- יחסים
- Foreign keys
- Cardinality
- אינדקסים חשובים
- Constraints משמעותיים

---

# 3. מודל הרשאות ותחומי אחריות

זהו **החלק הרגיש והחשוב ביותר במערכת**.

יש להפריד באופן מוחלט בין:

## שיוך

שיוך מתאר **לאיזו קבוצה המשתמש שייך**.

לדוגמה:

- צוות 8
- מגמת תוכנה
- התנסות א'
- סגל
- דתיים

למשתמש יכולות להיות מספר תגיות שיוך במקביל.

## תפקיד

תפקיד מתאר **מה המשתמש רשאי לעשות במערכת**.

לדוגמה:

- ממ״ש צוות 8
- קפ״מ מגמה א'
- מה״מ
- אחראי התנסות
- קצין דת
- מוביל AI

למשתמש יכולים להיות מספר תפקידים במקביל.

---

## תחום אחריות

לכל תפקיד מוגדר **Scope / Responsibility Scope**.

תחום האחריות קובע על אילו משתמשים בעל התפקיד רשאי:

- לפרסם משימות
- לעקוב אחר ביצוע
- לצפות בנתוני משתמשים
- לנהל פורומים

תחום אחריות יכול להיות מוגדר לפי:

- תגית אחת
- מספר תגיות
- קבוצה מסוימת
- מגמה שלמה
- כלל המשתמשים
- משתמשים שנבחרו ידנית
- שילוב של קבוצות ומשתמשים ידניים

### דוגמאות

- `ממ״ש צוות 8` → צוות 8
- `קפ״מ מגמה א'` → כל משתמשי מגמה א'
- `מה״מ` → כלל המשתמשים
- `אחראי התנסות` → משתמשי התנסות מסוימת
- `קצין דת` → קהל רלוונטי שהוגדר במערכת

---

## כלל הרשאה מרכזי: מעקב אינו תלוי במפרסם

הרשאת מעקב אחר ביצוע משימה **אינה תלויה במי שפרסם את המשימה**.

בעל תפקיד יכול לראות ביצוע של כל משימה אשר נשלחה למשתמשים שנמצאים בתחום האחריות שלו.

לדוגמה:

- מה״צ מפרסמת משימה לכל ההשלמה.
- ממ״ש צוות 8 לא פרסם את המשימה.
- מכיוון שחברי צוות 8 קיבלו אותה, ממ״ש צוות 8 יכול לראות את סטטוס הביצוע שלהם בלבד.

לדוגמה:

```text
צוות 8 – 6/10 ביצעו
```

ובפירוט:

```text
✓ נועה — בוצע
✓ דניאל — בוצע
○ יובל — טרם בוצע
○ מאיה — טרם בוצע
```

לעומת זאת, מה״מ בעלת Scope רחב יותר יכולה לראות:

```text
63/80 ביצעו

צוות 8 — 6/10
צוות 7 — 8/10
צוות 6 — 7/10
```

### כלל אבטחה מחייב

לעולם אין להחזיר ללקוח נתוני ביצוע של משתמש שאינו נמצא בתחום האחריות של אחד מהתפקידים הפעילים של המשתמש המבקש.

יש לאכוף זאת ב-Backend, ולא להסתמך על הסתרה ב-Frontend.

---

## הרשאות לפי תפקיד

יש לתמוך לפחות בהרשאות הבאות:

- `canPublishTasks`
- `canTrackTaskCompletion`
- `canManageForums`
- `canCreateForums`
- `canManageUsers`
- `canManageRoles`
- `canManageTags`

הרשאות צריכות להיות ניתנות להגדרה דינמית ממסך הניהול.

אין לקודד תפקידים ספציפיים בתוך הקוד.

---

# 4. API / Backend

הצג את ה-endpoints המרכזיים.

לכל endpoint יש לציין:

- HTTP Method
- Path
- Input
- Output
- Authentication
- Authorization rule
- Scope filtering

יש לכלול לפחות APIs עבור:

## משתמש

- קבלת המשתמש הנוכחי
- קבלת תגיות המשתמש
- קבלת תפקידי המשתמש

## משימות

- קבלת המשימות שלי
- קבלת משימה בודדת
- סימון משימה כ-"נצפתה"
- סימון משימה כ-"בוצעה"
- ביטול סימון "בוצעה"
- יצירת משימה
- עריכת משימה
- קבלת משימות לניהול
- קבלת סטטיסטיקת ביצוע
- קבלת רשימת מבצעים / לא מבצעים

## פורומים

- קבלת הפורומים שלי
- קבלת Feed של פורום
- יצירת פרסום
- יצירת משימה מתוך פורום
- עריכת פרסום
- מחיקת פרסום, אם מותר

## ניהול

- יצירת תגית
- עריכת תגית
- יצירת תפקיד
- עריכת תפקיד
- הגדרת Scope לתפקיד
- הקצאת תפקיד למשתמש
- הסרת תפקיד
- יצירת פורום
- הגדרת מנהלי פורום

---

# 5. Frontend

בנה Frontend עובד עבור המסכים המרכזיים.

כל הממשק:

- בעברית
- RTL
- Mobile-first
- Responsive

במובייל השתמש ב-**Bottom Navigation**.

בדסקטופ השתמש ב-**Sidebar**.

---

# מסמך הדרישות — PRD

## 1. משתמשים ושיוכים

לכל משתמש יכולות להיות מספר תגיות שיוך במקביל.

לדוגמה:

- צוות
- מגמה
- התנסות
- סגל
- דתיים
- קבוצות נוספות

משתמש יכול להיות משויך בו-זמנית ל:

- צוות 8
- מגמת תכנות
- התנסות א'

התגיות קובעות:

- אילו משימות מיועדות למשתמש
- אילו קבוצות רלוונטיות עבורו
- אילו פורומים הוא רואה

תגיות אינן הרשאות.

---

# 2. תפקידים והרשאות

משתמש יכול להשתייך לקבוצות שונות ובמקביל להחזיק במספר תפקידים.

יש להפריד בין:

### שיוך

לאיזו קבוצה המשתמש שייך.

### תפקיד

מה המשתמש רשאי לנהל.

לכל תפקיד מוגדר טווח אחריות שקובע על מי הוא יכול לפעול.

טווח האחריות יכול להיות:

- צוות מסוים
- מגמה שלמה
- כלל המשתמשים
- קבוצה ייעודית
- משתמשים שנבחרו ידנית

בעל התפקיד יכול, בהתאם להרשאות התפקיד:

- לפרסם משימות לטווח האחריות שלו
- לעקוב אחר ביצועי המשתמשים בטווח האחריות שלו
- לנהל פורומים שהוגדרו לתפקיד

### דוגמאות

#### ממ״ש צוות 8

- פרסום לצוות 8
- מעקב אחר צוות 8
- ניהול פורום צוות 8

#### קפ״מ מגמה א'

- פרסום לכל מגמה א'
- מעקב אחר כלל משתמשי המגמה

#### מה״מ / סגל

- פרסום לכלל המשתמשים
- מעקב בהתאם להרשאה שהוגדרה

#### אחראי התנסות

- פרסום ומעקב על משתמשים או קבוצות שהוגדרו עבורו

#### קצין דת / מוביל AI

- פרסום לקהל הרלוונטי
- ניהול פורום התחום
- ללא הרשאת מעקב, אם כך הוגדר

---

# 3. פורומים והרשאות

פורום יכול להיות:

- פורום קבוצתי
- פורום תחומי
- פורום כללי

### פורום קבוצתי

גלוי רק למשתמשי הקבוצה.

### פורום כללי / תחומי

יכול להיות גלוי לכל המשתמשים או לקבוצות שהוגדרו.

לכל תפקיד ניתן להגדיר אילו פורומים הוא רשאי לנהל.

---

# 4. נראות

משתמש רגיל רואה:

- רק משימות שנשלחו לקהל שאליו הוא משתייך
- פורומים בהתאם לתגיות שלו
- פורומים כלליים המוגדרים כגלויים עבורו

בעל תפקיד רואה:

- נתוני ביצוע רק של משתמשים בתחום האחריות שלו
- משימות לניהול לפי חפיפה בין קהל היעד של המשימה לבין תחום האחריות שלו

---

# 5. גמישות ניהולית

כל מנגנון התפקידים, התחומים והקבוצות חייב להיות מנוהל ללא שינוי קוד.

ממסך הניהול ניתן:

- ליצור תגיות
- לערוך תגיות
- ליצור תפקידים
- לערוך תפקידים
- להגדיר תחומי אחריות
- להקצות תפקידים למשתמשים
- ליצור פורומים
- להגדיר מי מנהל כל פורום
- להגדיר הרשאות לכל תפקיד

המטרה:

> כל בעל תפקיד מפרסם, מנהל ועוקב רק במסגרת האחריות שהוגדרה לו.

---

# 6. מסך הבית

מסך הבית צריך לתת תמונת מצב מיידית.

המשתמש צריך להבין בתוך מספר שניות:

> מה חדש → מה אני צריך לבצע → עד מתי

## חדש בשבילי

הצג משימות והודעות שהמשתמש עדיין לא פתח.

יש להבחין בין שני סטטוסים נפרדים:

### לא נצפה

המשתמש עדיין לא נכנס לפרסום.

### לא בוצע

המשתמש עדיין לא השלים את המשימה.

כניסה למשימה:

- מסמנת אותה כ-"נצפתה"
- מסירה את אינדיקציית "חדש"
- אינה מסמנת אותה כבוצעה

---

## המשימות שלי — Preview

הצג את המשימות הפתוחות והקרובות ביותר.

כל משימה תציג:

- Checkbox
- שם המשימה
- קבוצה / נושא
- תאריך יעד
- סטטוס
- אינדיקציה אם היא חדשה
- אינדיקציה אם היא דחופה
- אינדיקציה אם היא באיחור

### משימה דחופה

משימה נחשבת דחופה אם תאריך היעד שלה הוא בתוך היומיים הקרובים.

---

## עדכונים

הצג עדכונים אחרונים מהפורומים שאליהם המשתמש משתייך.

---

# 7. מסך "המשימות שלי"

Checklist המרכז את כל המשימות הרלוונטיות למשתמש.

המשימות יקובצו לפי:

- נושא
- קבוצה

יש לאפשר סינון לפי:

- הכל
- פתוחות
- בוצעו
- באיחור
- קבוצה
- נושא

כל משימה תציג:

- שם
- תאריך יעד
- קבוצה
- סטטוס ביצוע
- אינדיקציית חדש
- אינדיקציית דחוף / באיחור

---

# 8. מסך משימה

הצג:

- כותרת
- תיאור
- המפרסם
- התפקיד שממנו פורסמה
- קהל היעד
- תאריך פרסום
- תאריך יעד
- קישור לביצוע, אם קיים
- מסמכים רלוונטיים
- כפתור "סימון כבוצע"

אם המשתמש כבר סימן אותה כבוצעה:

- הצג סטטוס ברור
- אפשר לבטל את הסימון אם המדיניות מאפשרת זאת

---

# 9. יצירת משימה

רק משתמש בעל תפקיד מתאים יכול ליצור משימה.

אם למשתמש מספר תפקידים, הצג:

```text
פרסום בתור: [תפקיד]
```

הטופס כולל:

- כותרת
- תיאור
- תאריך יעד
- קישור אופציונלי
- מסמכים אופציונליים
- בחירת קהל יעד

## קהל יעד

קהל היעד ניתן לבחירה רק מתוך קבוצות / משתמשים שהתפקיד הנבחר מורשה לפרסם אליהם.

ניתן לבחור מספר קבוצות במקביל.

אין לאפשר ל-Frontend לשלוח קהל מחוץ ל-Scope ולסמוך על UI בלבד.

ה-Backend חייב לאמת שהקהל נמצא בתחום ההפצה של התפקיד.

---

# 10. מעקב אחר ביצוע משימות

הרשאת מעקב אינה תלויה במי שפרסם את המשימה.

בעל תפקיד יכול לעקוב אחר כל משימה שהופצה למשתמשים בתחום האחריות שלו.

## דוגמה

מה״צ מפרסמת משימה לכל ההשלמה.

ממ״ש צוות 8 לא פרסם את המשימה, אך יכול לראות:

```text
צוות 8 — 6/10 ביצעו
```

ובתוך המסך:

```text
✓ נועה — בוצע
✓ דניאל — בוצע
○ יובל — טרם בוצע
○ מאיה — טרם בוצע
```

מה״מ יכולה לראות:

```text
63/80 ביצעו
```

ולפי קבוצות:

```text
צוות 8 — 6/10
צוות 7 — 8/10
צוות 6 — 7/10
```

### כלל מחייב

כל בעל תפקיד רואה **רק** את נתוני המשתמשים הנמצאים בתחום האחריות שלו.

אם משימה נשלחה ל-80 אנשים אך Scope של בעל התפקיד כולל רק 10 מהם:

- ה-API יחזיר רק את 10 המשתמשים
- Progress יוצג כ-X/10
- אין לחשוף נתונים על 70 המשתמשים האחרים

---

# 11. מסך ניהול

מסך הניהול זמין לבעלי תפקידים עם הרשאות ניהול.

יש להציג בו את כל המשימות שיש להן חפיפה עם תחום האחריות של המשתמש.

אין להגביל את המסך למשימות שהמשתמש עצמו פרסם.

כל משימה תציג:

- שם
- מי פרסם
- באיזה תפקיד פורסמה
- קהל יעד
- תאריך יעד
- כמה מתוך האנשים שבתחום האחריות שלי ביצעו
- Progress bar
- כפתור "צפייה בביצוע"

לדוגמה:

```text
מילוי טופס העדפות
פורסם על ידי: רוני | מה״צ
יעד: כל ההשלמה
ביצוע בתחום שלי: 6/10
[██████----] 60%
```

אם המשתמש הוא המפרסם של המשימה, הצג גם:

```text
עריכת משימה
```

---

# 12. פורומים

לכל תגית / קבוצה יכול להיות פורום משויך.

לדוגמה:

- צוות 8
- מגמת תוכנה
- התנסות א'
- סגל

המשתמש רואה אוטומטית את הפורומים בהתאם לתגיות השיוך שלו.

ניתן בנוסף ליצור פורומים כלליים או תחומיים.

---

# 13. מסך פורום

הפורום הוא **Feed מסודר של פרסומים ולא Chat**.

אין לעצב אותו כמו WhatsApp, Messenger או אפליקציית הודעות.

כל פרסום יציג:

- מפרסם
- תפקיד המפרסם
- זמן
- תוכן
- סוג הפרסום

יש שני סוגים עיקריים.

## הודעה

מידע בלבד.

## משימה

דורשת פעולה וכוללת:

- תאריך יעד
- קהל יעד
- סטטוס ביצוע למשתמש

משימה שמתפרסמת בפורום נכנסת אוטומטית ל-"המשימות שלי" של קהל היעד.

מנהלי הפורום נקבעים לפי תפקיד.

לדוגמה:

`ממ״ש צוות 8` יכול לנהל את פורום צוות 8 ולפרסם בו:

- הודעות
- משימות

---

# 14. עריכת משימות

מפרסם המשימה יכול לערוך אותה לאחר הפרסום.

ניתן לערוך:

- כותרת
- תיאור
- תאריך יעד
- קישור
- קהל יעד

יש להציג:

```text
נערך לאחרונה: [תאריך ושעה]
```

---

## שינוי משמעותי

אם משימה שכבר נצפתה השתנתה באופן משמעותי, יש להציג אותה מחדש לנמענים עם Badge:

```text
עודכן
```

שינוי משמעותי כולל לפחות:

- שינוי תאריך יעד
- שינוי תיאור מהותי
- שינוי קהל יעד
- שינוי קישור לביצוע

יש לתכנן מנגנון המאפשר לזהות עדכון כזה ולשקף אותו למשתמשים.

---

# 15. סטטוסי משימה

יש להפריד בין סטטוס צפייה לבין סטטוס ביצוע.

לדוגמה, עבור כל משתמש ומשימה ניתן לשמור:

```text
viewedAt
completedAt
lastSeenVersion
```

אפשרות נוספת היא להשתמש בטבלת `TaskUserStatus`.

## מצבים אפשריים

### חדש ולא בוצע

```text
viewedAt = null
completedAt = null
```

### נצפה ולא בוצע

```text
viewedAt != null
completedAt = null
```

### נצפה ובוצע

```text
viewedAt != null
completedAt != null
```

אין להסיק ביצוע מתוך צפייה.

---

# 16. ניווט

ניווט ראשי:

- בית
- המשימות שלי
- פורומים
- ניהול

## Mobile

השתמש ב-Bottom Navigation.

## Desktop

השתמש ב-Sidebar.

`ניהול` יוצג רק למשתמש שמחזיק לפחות תפקיד אחד עם הרשאת ניהול מתאימה.

---

# 17. שפה עיצובית

המערכת צריכה להיות:

- עברית RTL
- Mobile-first
- מודרנית
- נקייה
- מינימליסטית
- קלה מאוד לסריקה

השתמש ב:

- Cards למשימות ופרסומים
- Badges לקבוצות
- Badges לתפקידים
- Badges לסטטוסים
- Checkbox ברור למשימות
- Progress bars למעקב
- אינדיקציה בולטת לפריטים חדשים
- הדגשה ברורה למשימות דחופות
- הדגשה ברורה למשימות באיחור
- הפרדה ויזואלית בין הודעה למשימה

---

# 18. עקרונות UX מרכזיים

משתמש רגיל שנכנס למערכת צריך להבין בתוך מספר שניות:

```text
מה חדש
↓
מה אני צריך לבצע
↓
עד מתי
```

בעל תפקיד צריך להבין:

```text
מה פורסם לאנשים שלי
↓
כמה ביצעו
↓
מי עדיין לא ביצע
```

---

# 19. מסכים שיש לממש

יש לספק קוד עובד לפחות עבור:

1. מסך בית
2. מסך המשימות שלי
3. מסך פרטי משימה
4. מסך יצירת משימה
5. מסך עריכת משימה
6. מסך פורומים
7. מסך פורום
8. מסך ניהול
9. מסך מעקב ביצוע של משימה
10. מסך ניהול משתמשים / תגיות / תפקידים / Scopes

---

# 20. דרישות אבטחה

הרשאות חייבות להיאכף בשרת.

אין להסתמך על:

- הסתרת כפתור
- route guard בצד הלקוח בלבד
- סינון נתונים לאחר שהשרת כבר החזיר אותם

בכל endpoint רגיש יש לבצע:

1. Authentication
2. טעינת תפקידי המשתמש
3. טעינת ההרשאות
4. חישוב Scope
5. סינון / אימות הנתונים לפי Scope

השרת לא יחזיר מידע שאינו מורשה למשתמש.

---

# 21. הערות מימוש

בסוף הפתרון יש להוסיף סעיף קצר הכולל:

- הנחות שהונחו
- החלטות ארכיטקטוניות משמעותיות
- Trade-offs
- נקודות שדורשות החלטת מוצר
- מקרים קיצוניים שכדאי לבדוק
- שאלות שכדאי לוודא לפני Production

---

# אילוצים מחייבים

1. כל הממשק בעברית RTL.
2. Mobile-first ורק לאחר מכן התאמה לדסקטופ.
3. הפרדה מלאה בין תגית שיוך לבין תפקיד.
4. למשתמש יכולות להיות מספר תגיות ומספר תפקידים.
5. מעקב ביצוע מבוסס Scope ולא על זהות המפרסם.
6. בעל תפקיד רואה רק אנשים בתחום האחריות שלו.
7. "נצפה" ו-"בוצע" הם שני סטטוסים שונים לחלוטין.
8. כניסה למשימה אינה מסמנת אותה כבוצעה.
9. פורום הוא Feed ולא Chat.
10. תפקידים, הרשאות, קבוצות, Scopes ופורומים צריכים להיות ניתנים לניהול דינמי ללא פיתוח.
11. אין לקודד שמות תפקידים ספציפיים בלוגיקה העסקית.
12. כל הרשאה רגישה חייבת להיאכף ב-Backend.
